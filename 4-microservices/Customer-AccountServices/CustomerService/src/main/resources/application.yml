server:
  port: 9162
spring:
  application:
    name: CUSTOMER-SERVICE
eureka:
  instance:
    hostname: localhost
  client:
    registry-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:9150/eureka